import Mathlib
Whitespaces
In Lean 4, sometimes you need whitespaces. Not everytime.
--example: (x : ) you need it
--example: a + b =a+b both are true
example (a b: ℝ): a+b = b +                  a := by sorry
Goodbye!
